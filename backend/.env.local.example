# Local overrides (this file is gitignored).
# Copy .env to .env.local and edit as needed.

LISTEN_ADDR=:8080
PROW_UPSTREAM=https://prow.k8s.io
TESTGRID_UPSTREAM=https://testgrid.k8s.io
CORS_ALLOWED_ORIGINS=http://localhost:5173
